---
description: Process raw sources into wiki pages
---

Read the skill file at `~/.claude/skills/wiki-ingest/SKILL.md` for full instructions, then follow them exactly.

If the user provided arguments (like a filename), process that specific file. Otherwise, detect and process all unprocessed files in `raw/`.
